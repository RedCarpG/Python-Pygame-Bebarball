from win32api import GetSystemMetrics


# Main Volume
MAIN_VOLUME = 0.1

# Color
GRAY = (128, 128, 128)
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
COLOR_WAIT_SCREEN = BLACK
COLOR_BAR = WHITE
COLOR_BALL = WHITE

# Screen Size

SIZE = WIDTH, HEIGHT = (800, 450)
FULL_SIZE = FULL_W, FULL_H = (GetSystemMetrics(0), GetSystemMetrics(1))
SCALE = (FULL_W / WIDTH, FULL_H / HEIGHT)

BAR_WIDTH = 20
BAR_HEIGHT = 130
BAR_MAX_SPEED = [0, 4]
BAR_INIT_POSITION_1 = WIDTH // 10, HEIGHT // 2
BAR_INIT_POSITION_2 = 9 * WIDTH // 10, HEIGHT // 2

BALL_RAD = 10
BALL_INIT_SPEED = [2, 4]
BALL_MAX_SPEED = [10, 9]
BALL_INCREASE_SPEED = 1
BALL_INIT_POSITION = WIDTH // 2, HEIGHT // 2


BAR_WIDTH_FS = int(BAR_WIDTH * SCALE[0])
BAR_HEIGHT_FS = int(BAR_HEIGHT * SCALE[1])

BAR_MAX_SPEED_FS = (int(BAR_MAX_SPEED[0] * SCALE[0]), int(BAR_MAX_SPEED[1] * SCALE[1]))
BAR_INIT_POSITION_1_FS = FULL_W // 10, FULL_H // 2
BAR_INIT_POSITION_2_FS = 9 * FULL_W // 10, FULL_H // 2

BALL_RAD_FS = BALL_RAD * SCALE[0]
BALL_INIT_SPEED_FS = (int(BALL_INIT_SPEED[0] * SCALE[0]), int(BALL_INIT_SPEED[1] * SCALE[1]))
BALL_MAX_SPEED_FS = (int(BALL_MAX_SPEED[0] * SCALE[0]), int(BALL_MAX_SPEED[1] * SCALE[1]))
BALL_INCREASE_SPEED_FS = int(BALL_INCREASE_SPEED * SCALE[0])
BALL_INIT_POSITION_FS = FULL_W // 2, FULL_H // 2
