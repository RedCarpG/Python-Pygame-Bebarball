from win32api import GetSystemMetrics


# Main Volume
MAIN_VOLUME = 0.1

# Color
GRAY = (128, 128, 128)
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
COLOR_WAIT_SCREEN = BLACK
COLOR_BAR = WHITE
COLOR_BALL = WHITE

# Screen Size

SIZE = WIDTH, HEIGHT = (800, 450)

scale1 = GetSystemMetrics(0) / WIDTH
scale2 = GetSystemMetrics(1) / HEIGHT

SCALE = scale1 if scale1 < scale2 else scale2

FULL_SIZE = FULL_W, FULL_H = (int(WIDTH*SCALE), int(HEIGHT*SCALE))
#SCALE = (FULL_W / WIDTH, FULL_H / HEIGHT)

BAR_WIDTH = 20
BAR_HEIGHT = 130
BAR_MAX_SPEED = [0, 4]
BAR_INIT_POSITION_1 = WIDTH // 10, HEIGHT // 2
BAR_INIT_POSITION_2 = 9 * WIDTH // 10, HEIGHT // 2

BALL_RAD = 10
BALL_INIT_SPEED = [2, 4]
BALL_MAX_SPEED = [10, 9]
BALL_INCREASE_SPEED = 1
BALL_INIT_POSITION = WIDTH // 2, HEIGHT // 2


BAR_WIDTH_FS = round(BAR_WIDTH * SCALE)
BAR_HEIGHT_FS = round(BAR_HEIGHT * SCALE)

BAR_MAX_SPEED_FS = (round(BAR_MAX_SPEED[0] * SCALE), round(BAR_MAX_SPEED[1] * SCALE))
BAR_INIT_POSITION_1_FS = FULL_W // 10, FULL_H // 2
BAR_INIT_POSITION_2_FS = 9 * FULL_W // 10, FULL_H // 2

BALL_RAD_FS = BALL_RAD * SCALE
BALL_INIT_SPEED_FS = (round(BALL_INIT_SPEED[0] * SCALE), round(BALL_INIT_SPEED[1] * SCALE))
BALL_MAX_SPEED_FS = (round(BALL_MAX_SPEED[0] * SCALE), round(BALL_MAX_SPEED[1] * SCALE))
BALL_INCREASE_SPEED_FS = round(BALL_INCREASE_SPEED * SCALE)
BALL_INIT_POSITION_FS = FULL_W // 2, FULL_H // 2
